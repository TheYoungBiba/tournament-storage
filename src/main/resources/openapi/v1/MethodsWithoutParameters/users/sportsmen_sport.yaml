    post:
      tags:
        - user
      summary: "To add sportsman to new sport"
      requestBody:
        description: "The sportsman data"
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SportsmenSportRequest'
      responses:
        '201':
          description: "Opearation was successful"
        '400':
          description: "Operation was failed" 

components:
  schemas:
    SportsmenSportRequest:
      type: object
      required:
        - sport_id
        - sportsmen_id
        - rating
      properties:
        sport_id:
          type: integer
        sportsmen_id:
          type: integer
        category_id:
          type: integer
        role_id:
          type: integer
        rank:
          type: string
        license:
          type: string
        rating:
          type: number
          format: float