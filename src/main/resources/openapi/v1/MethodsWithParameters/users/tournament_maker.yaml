    post:
      tags:
        - user
      summary: "To add tournament maker role to user by id"
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        description: "The tournament maker data"
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TournamentMakerRequest'
      responses:
        '201':
          description: "Role was added"
        '400':
          description: "Operation was failed" 
          
    delete:
      tags:
        - user
      summary: "To delete tournament maker role by id"
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
          description: "User id"
      responses:
        '204':
          description: "Removable was successful"

components:
  schemas:
    TournamentMakerRequest:
      type: object
      required:
        - organization_id
      properties:
        organization_id:
          type: integer