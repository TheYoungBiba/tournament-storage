get:
  tags:
    - user
  summary: Show all the existed judges
  description: To get list of every judge from database
  responses:
    '200':
      description: operation was successful
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/JudgeResponse.yaml
    '400':
      description: operation was failed
